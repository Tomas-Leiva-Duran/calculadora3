import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.calculadora3',
  appName: 'calculadora3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
